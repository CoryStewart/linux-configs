"====[ Tab handling ]====
set tabstop=4       " Tab indentation levels every 4 columns
set softtabstop=4   " Fixes backspace key to remove 4 spaces
set shiftwidth=4    " Indent/Outdent by four columns
set expandtab       " Convert all tabs that are typed into spaces
set shiftround      " Always indent/outdent to nearest tabstop

set relativenumber  " + & - line numbers from cursor. Use :setnorelativenumber to shut off
set number          " Display line numbers
